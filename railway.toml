[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn src.adapix.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
